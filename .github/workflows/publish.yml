name: Legacy Tag Publish

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Publishing now runs via .github/workflows/release.yml (Changesets)."
