{
  "name": "@eusilvio/monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @eusilvio/cep-lookup && npm run build -w @eusilvio/cep-lookup-react",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
